import { Meteor } from 'meteor/meteor';
import {Tasks} from '../imports/api/tasks.js';
import {Bitacoras} from '../imports/api/bitacoras';

Meteor.startup(() => {
  // code to run on server at startup
 
  
  
});
